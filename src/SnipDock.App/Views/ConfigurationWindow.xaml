<Window x:Class="SnipDock.App.Views.ConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" 
        Height="280" Width="470"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Icon="/Resources/Icons/app.ico">
    
    <Window.Resources>
        <Style x:Key="ModernAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}" />
        <Style x:Key="ModernSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}" />
    </Window.Resources>

    <Border Background="{DynamicResource ThemeSurfaceBrush}" CornerRadius="14" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="4" Color="#000000" Opacity="0.24"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title Area (Draggable) -->
            <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown" Background="Transparent">
                <StackPanel Orientation="Horizontal">
                    <Image Source="/Resources/Icons/logo.png" Width="28" Height="28" Stretch="Uniform" Margin="0,0,10,0"/>
                    <TextBlock Text="SnipDock" Foreground="{DynamicResource ThemeTextPrimaryBrush}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Center" />
                </StackPanel>
                
                <Button Content="{Binding CloseText}" Click="Close_Click" Style="{StaticResource CloseIconButtonStyle}" HorizontalAlignment="Right" FontSize="14" ToolTip="{Binding CloseText}" />
            </Grid>

            <!-- Mode-Specific Instructions -->
            <StackPanel Grid.Row="1" Margin="0,15,0,10">
                <TextBlock Text="{Binding WindowTitle}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding GuideText}" Foreground="{DynamicResource ThemeTextSecondaryBrush}" FontSize="12.5" TextWrapping="Wrap" LineHeight="18"/>
            </StackPanel>

            <!-- Browse Path controls -->
            <Grid Grid.Row="2" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <TextBox Text="{Binding StoragePath, UpdateSourceTrigger=PropertyChanged}" 
                         Background="{DynamicResource ThemeInputBgBrush}" Foreground="{DynamicResource ThemeTextPrimaryBrush}" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1.2"
                         Padding="10,6" FontSize="12" VerticalContentAlignment="Center" CaretBrush="{DynamicResource ThemeTextPrimaryBrush}">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="6" />
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                
                <Button Grid.Column="1" Content="{Binding BrowseText}" Command="{Binding BrowseCommand}" 
                        Style="{StaticResource ModernSecondaryButtonStyle}" Padding="15,6" Margin="10,0,0,0" />
            </Grid>

            <!-- Footer: error and buttons -->
            <Grid Grid.Row="3" Margin="0,10,0,0">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#f87171" FontSize="11" 
                           VerticalAlignment="Center" TextWrapping="Wrap" HorizontalAlignment="Left" Width="220"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <!-- Cancel Button (Only shown in settings modification mode) -->
                    <Button Content="{Binding CancelText}" Command="{Binding CancelCommand}" 
                            Style="{StaticResource ModernSecondaryButtonStyle}" Padding="18,8" Margin="0,0,10,0"
                            Visibility="{Binding IsCancelButtonVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    
                    <!-- Confirm Button -->
                    <Button Content="{Binding SaveButtonText}" Command="{Binding SaveCommand}" 
                            Style="{StaticResource ModernAccentButtonStyle}" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
