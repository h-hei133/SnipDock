<Window x:Class="SnipDock.App.Views.FloatingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SnipDock-FloatingWindow" 
        Height="65" Width="65"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Window.Resources>
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource ThemeSurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="6">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="18" ShadowDepth="3" Color="#000000" Opacity="0.26"/>
                            </Border.Effect>
                            <ItemsPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" 
                                Background="Transparent" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <ContentPresenter ContentSource="Header" RecognizesAccessKey="True" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentColorBrush}" />
                                <Setter Property="Foreground" Value="#ffffff" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border x:Name="FloatingBall" CornerRadius="32" BorderBrush="{DynamicResource AccentColorBrush}" BorderThickness="1.5" Cursor="SizeAll"
            Background="{DynamicResource FloatingBallBgBrush}"
            PreviewMouseLeftButtonDown="Border_PreviewMouseLeftButtonDown" 
            PreviewMouseMove="Border_PreviewMouseMove" 
            PreviewMouseLeftButtonUp="Border_PreviewMouseLeftButtonUp">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Color="#000000" Opacity="0.35"/>
        </Border.Effect>
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Opacity" Value="0.96" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="1" />
                        <Setter Property="BorderThickness" Value="2" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        
        <Border.ContextMenu>
            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                <MenuItem Header="{Binding Loc[TogglePanel], Mode=OneWay}" Command="{Binding TogglePanelCommand}" />
                <MenuItem Header="{Binding Loc[ResetFloatingPosition], Mode=OneWay}" Command="{Binding ResetPositionCommand}" />
                <MenuItem Header="{Binding Loc[SetDataDirectory], Mode=OneWay}" Command="{Binding ResetStoragePathCommand}" />
                <Separator Background="{DynamicResource ThemeBorderBrush}" Margin="2,4" />
                <MenuItem Header="{Binding Loc[ExitApp], Mode=OneWay}" Command="{Binding ExitCommand}" />
            </ContextMenu>
        </Border.ContextMenu>
        
        <Grid IsHitTestVisible="False">
            <Ellipse Margin="7" Stroke="#55FFFFFF" StrokeThickness="1"/>
            <Border Width="34" Height="34" CornerRadius="12" Background="#22FFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="S" Foreground="#ffffff" FontSize="25" FontWeight="Black"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1.2" Color="#000000" Opacity="0.45"/>
                </TextBlock.Effect>
            </TextBlock>
        </Grid>
    </Border>
</Window>
