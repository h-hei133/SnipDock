<Window x:Class="SnipDock.App.Views.ConfirmDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SnipDock" Height="190" Width="380"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="/Resources/Icons/app.ico">
    
    <Window.Resources>
        <Style x:Key="DialogAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogPrimaryButtonStyle}" />
        <Style x:Key="DialogSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}" />
    </Window.Resources>

    <Border Background="{DynamicResource ThemeSurfaceBrush}" CornerRadius="12" BorderBrush="{DynamicResource ThemeBorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" ShadowDepth="4" Color="#000000" Opacity="0.28"/>
        </Border.Effect>
        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title Bar / Header (Draggable) -->
            <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown" Background="Transparent">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Border Width="22" Height="22" CornerRadius="7" Background="{DynamicResource AccentColorSelectedBgBrush}" BorderBrush="{DynamicResource AccentColorBrush}" BorderThickness="1" Margin="0,0,8,0">
                        <TextBlock Text="!" Foreground="{DynamicResource AccentColorTextBrush}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock x:Name="TxtTitle" Text="" Foreground="{DynamicResource ThemeTextPrimaryBrush}" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
                </StackPanel>
                
                <Button x:Name="BtnCloseCancel" Content="" Click="Cancel_Click" Style="{StaticResource CloseIconButtonStyle}" HorizontalAlignment="Right" FontSize="13" Width="30" Height="30" ToolTip="Close" />
            </Grid>

            <!-- Message Area -->
            <Grid Grid.Row="1" Margin="0,15,0,15">
                <TextBlock x:Name="TxtMessage" Text="" Foreground="{DynamicResource ThemeTextSecondaryBrush}" FontSize="12.5" LineHeight="18" TextWrapping="Wrap" VerticalAlignment="Center" />
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" Content="" Click="Cancel_Click" Style="{StaticResource DialogSecondaryButtonStyle}" Margin="0,0,10,0" />
                <Button x:Name="BtnConfirm" Content="" Click="Confirm_Click" Style="{StaticResource DialogAccentButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
